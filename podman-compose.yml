version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-https://nondelusive-unemotionally-tommye.ngrok-free.dev}
    ports:
      - "5173:5173"
    restart: unless-stopped
